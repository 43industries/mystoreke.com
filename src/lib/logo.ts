/**
 * Brand logo — file lives in `public/brand/` (served at `/brand/...`).
 */
export const BRAND_LOGO_SRC = "/brand/logo.png";

/**
 * Set `true` if the image already includes the business name/wordmark (no extra “Mystore” text).
 */
export const BRAND_LOGO_INCLUDES_WORDMARK = true;
